// contains declorations of unique methods + includes global methods
#pragma once

#include "box.h"

box* list_create(); // can't add method to box.h because creator is an unique method for every object
